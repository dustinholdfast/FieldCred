{
  "name": "fieldcred-billing-service",
  "private": true,
  "type": "module",
  "description": "Stripe Checkout -> tenant auto-provisioning webhook service (Handoff 04). Deploy this separately from the fieldcred PHP host (Render/Fly/a VPS) - it holds STRIPE_SECRET_KEY and SUPABASE_ACCESS_TOKEN, and per supabase/TENANCY-MODEL.md and the admin-dashboard's own header comment, those must never sit behind a shared/public web host. See README.md before deploying.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --env-file=.env.local server.mjs"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.0",
    "stripe": "^16.12.0"
  }
}
